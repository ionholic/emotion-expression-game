# 마음 얼굴 연구소 전반 점검 보고서

점검일: 2026-08-13

## 현재 판단

서비스의 핵심 흐름인 카메라 보정, 10개 표정 게임, 5초 표정 읽기, 연습 모드, 모바일 반응형 화면은 모두 작동한다. 영상과 얼굴 분석 결과를 서버에 저장하지 않고 실제 감정을 진단하지 않는다는 안내도 적절히 제공된다. 이번 점검에서는 기능 추가보다 카메라 요청의 안전성, 실패 복구, 결과 확인 시간, 모바일 조판, 백그라운드 연산을 우선 개선했다.

## 이번에 해결한 문제

| 중요도 | 발견 사항 | 적용한 개선 |
| --- | --- | --- |
| 높음 | 빠른 연속 실행이나 화면 이동 뒤 늦은 카메라 요청이 현재 상태를 덮을 수 있음 | 요청 식별자를 사용해 오래된 스트림을 즉시 종료하고 현재 요청만 반영 |
| 높음 | 라운드 결과가 1.7초 뒤 자동으로 사라져 충분히 읽기 어려움 | 사용자가 `다음 표정으로` 또는 마지막 라운드의 `탐색 결과 보기`를 눌러 이동하도록 변경 |
| 높음 | 결과가 열린 상태에서 세션이 바뀌면 뒤늦게 다음 라운드가 열릴 수 있음 | 세션 버전 검증과 모달 상태 초기화 추가 |
| 중간 | 카메라 권한·연결 실패 후 같은 화면에서 재시도할 수 없음 | 오류 원인 안내와 `카메라 다시 연결` 제공 |
| 중간 | 보이지 않는 영상까지 준비 단계에서 재생 완료를 기다림 | 준비 화면 영상만 우선 재생하고 실제 모드 진입 시 필요한 영상을 재생 |
| 중간 | 일시정지·결과 확인 중에도 얼굴 분석 연산이 계속됨 | 정지·백그라운드·결과 상태에서는 추론을 건너뛰어 발열과 배터리 사용 감소 |
| 중간 | 모바일에서 긴 혼합 결과 제목이 한 글자만 다음 줄로 떨어짐 | 긴 제목 전용 크기와 균형 조판 적용 |
| 낮음 | 결과 모달보다 토스트가 위에 표시되어 버튼을 가릴 수 있음 | 결과 모달을 최상위로 올리고 진입 시 토스트 닫기 |
| 낮음 | Pages 배포 액션이 Node.js 20 사용 중단 경고를 표시함 | Node.js 24 기반 공식 Pages 액션 버전으로 갱신 |

## 검증한 흐름

- 연습 모드 10개 표정 전체 완료와 최종 기록 10개 생성
- 결과 모달이 자동으로 사라지지 않는지, 다음 라운드 제목으로 포커스가 이동하는지 확인
- 모달이 열린 동안 배경 조작이 차단되는지 확인
- 중복 카메라 요청에서 이전 스트림이 종료되고 마지막 요청만 유지되는지 확인
- 권한 거부 안내, 재시도 버튼, 재연결 완료 상태 확인
- 중간 화면 이탈 뒤 예약된 다음 라운드가 열리지 않는지 확인
- 390×844 모바일에서 기본 결과와 긴 혼합 결과가 화면 안에 표시되는지 확인
- 콘솔 오류와 가로 화면 넘침이 없는지 확인

## 다음 개발 계획

### 1. 실제 기기·사용자 정확도 검증

아이폰 Safari와 안드로이드 Chrome에서 조명, 안경, 카메라 높이, 얼굴 움직임 차이를 포함한 실기기 시험을 진행한다. 아동 사용자의 실제 감정을 맞힌다는 목표가 아니라, 제시된 표정 동작을 일관되게 관찰하는지 측정한다. 감정별 성공률, 평균 소요 시간, 건너뛰기 비율을 기록하고 임계값을 조정한다.

### 2. 분석 자산 자체 호스팅

현재 얼굴 분석 라이브러리와 모델은 외부 CDN에서 최초 한 번 내려받는다. 버전 파일을 서비스 안에 포함해 외부 장애 영향을 줄이고, 캐시 정책과 파일 무결성을 관리한다. 이후 설치형 웹 앱과 제한적인 오프라인 안내 화면을 검토한다.

### 3. 자동 배포 품질 검사

GitHub Pages 배포 전에 시작 화면, 연습 모드, 결과 모달, 카메라 오류 복구를 확인하는 브라우저 검사를 자동 실행한다. JavaScript 문법 오류, 화면 넘침, 콘솔 오류가 있으면 배포를 중단하도록 구성한다.

### 4. 보호자·교사용 운영 기능

개인 얼굴 영상이나 원본 분석값을 저장하지 않는 원칙을 유지하면서, 기기 안에서만 라운드 완료 여부와 어려웠던 표정 정도를 선택적으로 요약하는 방식을 검토한다. 저장 기능을 추가한다면 명시적 동의, 삭제, 보관 기간을 먼저 설계한다.

## 출시 전 권장 기준

- iOS Safari와 Android Chrome 실제 기기에서 카메라 시작·앱 전환·회전·재시도 통과
- 감정별 시험 표본을 충분히 확보하고 특정 얼굴·조명 조건에서 실패가 몰리지 않는지 확인
- 외부 모델 로딩 실패 시 연습 모드와 재시도가 항상 가능할 것
- 모든 주요 버튼을 키보드와 화면 읽기로 사용할 수 있을 것
- 영상·랜드마크·감정 점수를 외부 서버로 전송하지 않는 현재 원칙을 유지할 것
